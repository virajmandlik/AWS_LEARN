{
  "name": "aws-docs-notion-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bi-weekly GitHub Actions workflow that watches AWS docs via the AWS Knowledge MCP and updates linked Notion pages using GitHub Models for AI-assisted merging.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "sync:dry": "DRY_RUN=1 node scripts/sync.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.20.2",
    "@notionhq/client": "5.2.0",
    "jsdom": "^27.0.0",
    "turndown": "^7.2.1"
  }
}
